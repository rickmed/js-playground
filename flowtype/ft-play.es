// @flow

import * as R from 'ramda'
import * as S from 'sanctuary'
import { add } from 'ramda'
import * as fs from 'fs'

const sumTest = (a: number, b: number) => a + b

console.log(sumTest('331', 321))

